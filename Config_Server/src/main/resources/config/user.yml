spring:
  mongodb:
    host: localhost
    database: ms_user_db
    port: 27017

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoint:
    busrefresh:
      access: unrestricted
    health:
      show-details: always
    shutdown:
      enabled: true

  endpoints:
    web:
      exposure:
        include: "*"

info:
  app:
    name: User
    version: 0.0.1
    owner: Saddab

server:
  port: 8082
